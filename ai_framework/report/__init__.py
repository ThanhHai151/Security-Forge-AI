"""Reporting/export formats for the advisory flow (SARIF, ...).

Deterministic renderers over the Hermes notebook — no AI-provider call, no target access —
so CI can consume SecForge findings the same way it consumes any code scanner's output.
"""
